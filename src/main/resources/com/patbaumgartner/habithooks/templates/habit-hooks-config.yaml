# .habit-hooks.yaml — generated by habit-hooks init
#
# Edit the analyzers and scope sections to fit your project.
# Full config reference: https://github.com/patbaumgartner/habbit-hooks#configuration
# Agent loop: habit-hooks doctor && habit-hooks --all && habit-hooks tasks --no-fail

prompts: ./prompts

rules:
  # Disable or tune rules here. Example:
  # checkstyle:TodoComment:
  #   disabled: true
  # pmd:GodClass:
  #   exclude:
  #     - "**/*Test.java"

scope:
  onlyChangedFiles: true
  branchBase: main
  excludeTests: true

analyzers:
  checkstyle:
    enabled: true
    configFile: checkstyle.xml
  pmd:
    enabled: true
    rulesets:
      - pmd-ruleset.xml
  taikai:
    enabled: false
    testClass: ArchitectureTest
  spotbugs:
    enabled: false
    goal: spotbugs:spotbugs
    reportFile: target/spotbugsXml.xml
  jacoco:
    enabled: false
    goal: test jacoco:report
    reportFile: target/site/jacoco/jacoco.xml
  cyclonedx:
    enabled: false
    goal: package cyclonedx:makeAggregateBom
    reportFile: target/bom.json
  pitest:
    enabled: false
    goal: -Pmutation-test test-compile org.pitest:pitest-maven:mutationCoverage
    reportFile: target/pit-reports/mutations.xml
  spring-javaformat:
    enabled: false
    goal: spring-javaformat:validate
    reportFile: target/habit-hooks/spring-javaformat.log
  errorprone:
    enabled: false
    goal: compile
    reportFile: target/habit-hooks/errorprone.log
  owasp:
    enabled: false
    goal: org.owasp:dependency-check-maven:check -Dformat=JSON
    reportFile: target/dependency-check-report.json
  jspecify:
    enabled: false
